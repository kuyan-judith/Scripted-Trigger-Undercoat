# Augmentation Bazaars Initial Setup
# Buildings Added:
#	* Augmentation Bazaars

if = {
	limit = {
		exists = owner
		owner = {
			is_augmentation_bazaars_empire = yes # undercoat: moved to scripted trigger
		}
	}
	add_building = building_augmentation_bazaars
}
